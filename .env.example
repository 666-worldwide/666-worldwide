# Server
PORT=3000
NODE_ENV=production

# Database (Railway provides this automatically when a Postgres plugin is attached)
DATABASE_URL=postgresql://user:password@host:5432/railway

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Uploads
MAX_UPLOAD_MB=5
